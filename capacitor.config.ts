import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.hangell.babycoruja',
  appName: 'Baby Coruja',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
